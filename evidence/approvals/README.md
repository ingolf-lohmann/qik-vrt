# Execution approval evidence

This directory stores explicit, human-authorized execution approvals used to establish a repository-native, auditable bridge when GitHub Actions blocks an otherwise reviewable pull request before any job starts.

Approval evidence never replaces required tests, reviews, integrity checks, formal proof receipts, or mirror synchronization. It only records authorization and preserves fail-closed traceability.
