# Observable Reality as an Infrared Evidence Spiral / Beobachtbare Wirklichkeit als infrarote Evidenzspirale

Author: Ingolf Lohmann  
Status: scientific integration candidate - 2026-09-03

## Scientific core

The integrated structure is:

```text
W_obs(E) = {admissible microscopic models} / observational_indistinguishability_at_E
Omega = Fix_N(A(lim_{n->infinity} F^(n)(0->1)))
E_(n+1) = Closure(E_n union DeltaE_(n+1))
E_n subseteq E_(n+1)
A(E_n*) = E_n*
```

The evidence chain is:

```text
distinction -> relation -> state -> effect -> observation
-> reproducible evidence -> exact reobservation -> local fixed point
-> successor evidence stage
```

The resulting construction is an **evidence spiral**: local closure does not mean final knowledge. A locally closed state is invariant under its declared admissibility/check operator, while a successor evidence state remains constructible.

## Fixed-point binding

The pre-existing QIK-VRT fixed-point structure is treated as an axiomatic structure theorem under its declared assumptions.

```text
QIKVRT_Fixpunktbeweis_final.pdf
SHA-256 bf6521828db3ea52d67868b1c8ba09b0c0256562f684231df6833b1f68c2d55e
related repository DOI reference: 10.5281/zenodo.20712301
```

This formal result is not silently promoted to unconditional empirical proof of the complete physical universe or quantum gravity. Observation maps, measurements, falsification criteria and independent reproduction remain separate obligations.

## Binary theorem and boundary

For every finite alphabet `A` there exists an injective encoding

```text
enc : A* -> {0,1}*
```

so every finite symbolic evidence record admits lossless finite binary representation. This representation theorem does not by itself establish finite-binary physical ontology for continuous fields, arbitrary real values or unrestricted quantum states.

## Causal precedence theorem/contract

Scientific evidence is not earned by unordered completion. The repository publication-control path is a strict partial order:

```text
P0 MANIFEST latest accepted knowledge
 -> P1 BUILD one exact integration head
 -> P2 VALIDATE that exact head
 -> P3 REVIEW that same validated head
 -> P4 REOBSERVE the same post-review head
 -> P5 PROMOTE legitimately to one exact Trusted Main SHA
 -> P6 REOBSERVE that exact resulting Main SHA
 -> P7 BIND and verify external obligations to that Main SHA
 -> independent fanout:
      Wikipedia request -> authoritative readback
      Zenodo publish -> record/DOI/file-hash readback
      arXiv authenticated submit -> submission/status readback -> later public-ID/version readback
```

Unspecified precedence is `HOLD_UNVERIFIED`. Order must not be inferred from timestamps, PR numbers, file ordering, or predecessor success.

## World-formula condensate

> Fundamental is not every imagined microscopic difference, but a difference that becomes relationally defined, state-bearing, causally effective, reproducibly evidenced, and remains admissible under exact reobservation. Closure is local; scientific continuation remains open.

Model-level condensate:

```text
ROUNDTRIP = Fix_N o A o lim F^(n) o (0->1)
```

with monotone successor construction after every local closure.

## Epistemic invariants

```text
finite binary encodability != binary physical ontology
formal theorem != empirical law of nature
tested implementation != physical confirmation
transport ACK != effect ACK
local fixed point != final state of knowledge
repository success != external publication
```

## Current repository consequence

This scientific consolidation is itself P0/P1 material. It must be included in the exact integration head before validation, review, promotion, Main reobservation, or any new external publication effect may count. Any head mutation invalidates predecessor-head validation/review evidence for promotion purposes and requires fresh exact-head evidence.
