# QIK-VRT mandatory handoff

Read `/AI` before performing project work. Load `AI_CONTEXT.json`, follow `required_read_order`, verify repository identity and state, and continue only from verified repository evidence. Preserve effect, integrity, provenance, licensing, and symmetric-canonicality boundaries.
