name: QIKVRT Node Renew Registration
on:
  workflow_dispatch:
    inputs:
      run_id:
        description: QIKVRT run id
        required: false
        type: string
  schedule:
    - cron: '3 */6 * * *'
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      QIKVRT_RUN_ID: ${{ inputs.run_id || github.run_id }}
    steps:
      - uses: actions/checkout@v4
      - name: Run QIKVRT Node Renew Registration
        run: sh tools/qikvrt_node_renew_registration.sh
      - name: Commit QIKVRT outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add qikvrt/runtime/onboarding evidence || true
          if git diff --cached --quiet; then
            echo "QIKVRT_NO_CHANGES"
          else
            git commit -m "QIKVRT 4AU QIKVRT Node Renew Registration $QIKVRT_RUN_ID"
            git push
          fi
