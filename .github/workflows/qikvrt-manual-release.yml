name: QIKVRT V45.12 Manual Evidence Freeze

on:
  workflow_dispatch:

jobs:
  local-structure-note:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Evidence freeze policy note
        run: |
          echo "QIKVRT V45.12: no force-tag update, no release asset clobber."
          echo "Real release is driven by Windows wrapper with Product Owner acceptance."
