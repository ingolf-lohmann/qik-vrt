# SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
# Copyright 2026 Ingolf Lohmann.
name: QIK-VRT expected-head promotion executor

on:
  workflow_dispatch:
    inputs:
      pr:
        description: "Optional PR number; still requires the repository promotion marker"
        required: false
        type: string
  schedule:
    - cron: "*/10 * * * *"
  workflow_run:
    workflows:
      - "QIKVRT CI"
      - "QIKVRT repository evidence materialization"
      - "QIKVRT Collective Proposal Review"
      - "QIK-VRT global claim completion"
      - "QIKVRT Batch-003 remaining subject disposition"
      - "QIK-VRT universal ontology finite-model verification"
      - "QIK-VRT measurement-independence boundary verification"
      - "QIK-VRT manuscript proof coverage"
      - "QIK-VRT autonomous exact-head verification"
    types: [completed]

permissions:
  actions: read
  contents: write
  issues: write
  pull-requests: write
  statuses: read

concurrency:
  group: qikvrt-expected-head-promotion-${{ github.repository }}
  cancel-in-progress: false

jobs:
  evaluate-and-promote:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ github.token }}
      REPOSITORY: ${{ github.repository }}
      PROMOTION_MARKER: "<!-- qikvrt-expected-head-promotion:enabled external_effect=NONE -->"
      EXACT_HEAD_VERIFIER_CONTEXT: "QIK-VRT autonomous exact-head verification"
      REQUIRED_GATES_JSON: >-
        ["QIKVRT CI","QIKVRT repository evidence materialization","QIKVRT Collective Proposal Review","QIK-VRT global claim completion"]
    steps:
      - name: Check out current main promotion code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: main
          fetch-depth: 1
          persist-credentials: false

      - name: Regression-test promotion decision core
        shell: bash
        run: |
          set -euo pipefail
          python3 -B -m unittest -v tests.test_qikvrt_expected_head_promotion

      - name: Discover one marked current-base candidate
        id: candidate
        env:
          REQUESTED_PR: ${{ inputs.pr || '' }}
        shell: bash
        run: |
          set -euo pipefail
          python3 -B - <<'PY' > /tmp/qikvrt-promotion-candidate.json
          import json
          import os
          import subprocess

          repository = os.environ['REPOSITORY']
          marker = os.environ['PROMOTION_MARKER']
          requested = os.environ.get('REQUESTED_PR', '').strip()

          def gh_json(path):
              raw = subprocess.check_output(['gh', 'api', path], text=True)
              return json.loads(raw)

          def gh_pages(path):
              raw = subprocess.check_output(
                  ['gh', 'api', '--paginate', '--slurp', path], text=True
              )
              pages = json.loads(raw)
              result = []
              for page in pages:
                  if isinstance(page, list):
                      result.extend(page)
                  else:
                      result.append(page)
              return result

          current_main = gh_json(f'repos/{repository}/commits/main')['sha']
          if requested:
              prs = [gh_json(f'repos/{repository}/pulls/{int(requested)}')]
          else:
              prs = gh_pages(f'repos/{repository}/pulls?state=open&base=main&per_page=100')

          candidates = []
          for pr in prs:
              if pr.get('state') != 'open' or pr.get('base', {}).get('ref') != 'main':
                  continue
              # A stale-base PR is not a current writer and cannot be promoted.
              if pr.get('base', {}).get('sha') != current_main:
                  continue
              head_repository = ((pr.get('head') or {}).get('repo') or {}).get('full_name')
              head_ref = pr.get('head', {}).get('ref')
              body = pr.get('body') or ''
              if (
                  head_repository != repository
                  or not isinstance(head_ref, str)
                  or not head_ref.startswith('automation/self-heal-')
                  or marker not in body
                  or '<!-- qikvrt-autonomous-self-heal:enabled -->' not in body
              ):
                  continue
              candidates.append(pr)

          # Serialize by the oldest explicitly marked current-base candidate.
          candidates.sort(key=lambda pr: (pr.get('created_at') or '', pr['number']))
          chosen = candidates[0] if candidates else None
          print(json.dumps({
              'state': 'CANDIDATE' if chosen else 'NOOP',
              'current_main_sha': current_main,
              'pr_number': chosen['number'] if chosen else None,
          }, sort_keys=True))
          PY
          cat /tmp/qikvrt-promotion-candidate.json
          python3 -B - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          value = json.load(open('/tmp/qikvrt-promotion-candidate.json', encoding='utf-8'))
          print(f"state={value['state']}")
          print(f"pr={value.get('pr_number') or ''}")
          print(f"main_sha={value['current_main_sha']}")
          PY

      - name: Materialize exact live promotion snapshot
        if: steps.candidate.outputs.state == 'CANDIDATE'
        id: decision
        env:
          PR_NUMBER: ${{ steps.candidate.outputs.pr }}
        shell: bash
        run: |
          set -euo pipefail
          python3 -B - <<'PY' > /tmp/qikvrt-promotion-snapshot.json
          import json
          import os
          import subprocess

          repository = os.environ['REPOSITORY']
          pr_number = int(os.environ['PR_NUMBER'])
          required_gates = json.loads(os.environ['REQUIRED_GATES_JSON'])
          verifier_context = os.environ['EXACT_HEAD_VERIFIER_CONTEXT']
          github_server_url = os.environ['GITHUB_SERVER_URL'].rstrip('/')

          def gh_json(path):
              raw = subprocess.check_output(['gh', 'api', path], text=True)
              return json.loads(raw)

          def gh_pages(path):
              raw = subprocess.check_output(
                  ['gh', 'api', '--paginate', '--slurp', path], text=True
              )
              pages = json.loads(raw)
              result = []
              for page in pages:
                  if isinstance(page, list):
                      result.extend(page)
                  else:
                      result.append(page)
              return result

          def gh_run_pages(path):
              raw = subprocess.check_output(
                  ['gh', 'api', '--paginate', '--slurp', path], text=True
              )
              pages = json.loads(raw)
              runs = []
              for page in pages:
                  if isinstance(page, dict):
                      runs.extend(page.get('workflow_runs', []))
              return runs

          def workflow_run_from_status(status):
              target_url = status.get('target_url')
              prefix = f'{github_server_url}/{repository}/actions/runs/'
              if not isinstance(target_url, str) or not target_url.startswith(prefix):
                  return None
              run_id = target_url.removeprefix(prefix)
              if not run_id.isascii() or not run_id.isdecimal() or int(run_id) <= 0:
                  return None
              try:
                  run = gh_json(f'repos/{repository}/actions/runs/{run_id}')
              except subprocess.CalledProcessError:
                  return None
              run_repository = run.get('repository')
              head_repository = run.get('head_repository')
              return {
                  'id': run.get('id'),
                  'html_url': run.get('html_url'),
                  'name': run.get('name'),
                  'path': run.get('path'),
                  'event': run.get('event'),
                  'status': run.get('status'),
                  'conclusion': run.get('conclusion'),
                  'head_sha': run.get('head_sha'),
                  'head_branch': run.get('head_branch'),
                  'repository': (
                      run_repository.get('full_name')
                      if isinstance(run_repository, dict)
                      else None
                  ),
                  'head_repository': (
                      head_repository.get('full_name')
                      if isinstance(head_repository, dict)
                      else None
                  ),
              }

          pr = gh_json(f'repos/{repository}/pulls/{pr_number}')
          current_main = gh_json(f'repos/{repository}/commits/main')['sha']
          head = pr['head']['sha']
          runs = gh_run_pages(
              f'repos/{repository}/actions/runs?head_sha={head}&per_page=100'
          )
          workflow_runs = [
              {
                  'name': run.get('name'),
                  'status': run.get('status'),
                  'conclusion': run.get('conclusion'),
                  'run_number': run.get('run_number'),
                  'event': run.get('event'),
              }
              for run in runs
          ]
          exact_head_verifier_statuses = [
              {
                  'id': status.get('id'),
                  'sha': status.get('sha'),
                  'context': status.get('context'),
                  'state': status.get('state'),
                  'description': status.get('description'),
                  'target_url': status.get('target_url'),
                  'workflow_run': workflow_run_from_status(status),
              }
              for status in gh_pages(
                  f'repos/{repository}/commits/{head}/statuses?per_page=100'
              )
              if status.get('context') == verifier_context
          ]

          candidate_files = {
              path
              for item in gh_pages(f'repos/{repository}/pulls/{pr_number}/files?per_page=100')
              for path in (item.get('filename'), item.get('previous_filename'))
              if isinstance(path, str) and path
          }
          contract = json.load(
              open('state/autonomy/AUTONOMOUS_SELF_HEALING_CONTRACT_V1.json', encoding='utf-8')
          )
          allowed_paths = sorted({
              path
              for handler in contract['allowlisted_handlers']
              for path in handler['mutable_paths']
          })
          overlaps = []
          for other in gh_pages(f'repos/{repository}/pulls?state=open&base=main&per_page=100'):
              if other['number'] == pr_number:
                  continue
              # Old-base PRs cannot write the current main without first
              # reconciling their base, so they are not current competing writers.
              if other.get('base', {}).get('sha') != current_main:
                  continue
              if other.get('head', {}).get('sha') == head:
                  # Trusted verification proxies share the exact candidate head
                  # and are not independent writers.
                  continue
              other_files = {
                  path
                  for item in gh_pages(
                      f"repos/{repository}/pulls/{other['number']}/files?per_page=100"
                  )
                  for path in (item.get('filename'), item.get('previous_filename'))
                  if isinstance(path, str) and path
              }
              common = sorted(candidate_files & other_files)
              if common:
                  overlaps.append({'pr_number': other['number'], 'paths': common})

          print(json.dumps({
              'pr_number': pr_number,
              'repository': repository,
              'github_server_url': github_server_url,
              'current_main_sha': current_main,
              'base_sha': pr['base']['sha'],
              'expected_head_sha': head,
              'current_head_sha': head,
              'commit_status_sha': head,
              'draft': bool(pr.get('draft')),
              'mergeable': pr.get('mergeable') is True,
              'external_effect': 'NONE',
              'same_repository': (
                  ((pr.get('head') or {}).get('repo') or {}).get('full_name')
                  == repository
              ),
              'marker_in_pr_body': os.environ['PROMOTION_MARKER'] in (pr.get('body') or ''),
              'head_ref': pr['head']['ref'],
              'candidate_files': sorted(candidate_files),
              'allowed_paths': allowed_paths,
              'required_gates': required_gates,
              'workflow_runs': workflow_runs,
              'exact_head_verifier_statuses': exact_head_verifier_statuses,
              'competing_writer_overlaps': overlaps,
          }, sort_keys=True))
          PY

          set +e
          python3 -B tools/qikvrt_expected_head_promotion.py evaluate \
            --input /tmp/qikvrt-promotion-snapshot.json \
            > /tmp/qikvrt-promotion-decision.json
          rc=$?
          set -e
          cat /tmp/qikvrt-promotion-decision.json
          python3 -B - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          value = json.load(open('/tmp/qikvrt-promotion-decision.json', encoding='utf-8'))
          print(f"state={value['state']}")
          print(f"head={value.get('expected_head_sha') or ''}")
          print(f"blocker={value.get('first_blocker') or ''}")
          PY
          if [ "$rc" -ne 0 ]; then
            exit 0
          fi

      - name: Reobserve exact unchanged head and advance one promotion phase
        if: steps.decision.outputs.state == 'PROMOTABLE'
        env:
          PR_NUMBER: ${{ steps.candidate.outputs.pr }}
          EXPECTED_HEAD: ${{ steps.decision.outputs.head }}
          EXPECTED_BASE: ${{ steps.candidate.outputs.main_sha }}
        shell: bash
        run: |
          set -euo pipefail
          current_main="$(gh api "repos/${REPOSITORY}/commits/main" --jq .sha)"
          current_head="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}" --jq .head.sha)"
          current_base="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}" --jq .base.sha)"
          merged="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}" --jq .merged)"
          draft="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}" --jq .draft)"
          test "$merged" = "false"
          test "$current_main" = "$EXPECTED_BASE"
          test "$current_base" = "$EXPECTED_BASE"
          test "$current_head" = "$EXPECTED_HEAD"

          if [ "$draft" = "true" ]; then
            # Phase 1 only. ready_for_review may itself register new checks, so
            # this run MUST NOT merge. A later workflow_run/schedule cycle will
            # rebuild the exact-head gate snapshot before Phase 2.
            gh pr ready "$PR_NUMBER" --repo "$REPOSITORY"
            echo "READY_RECLASSIFIED_REOBSERVATION_REQUIRED" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          # Phase 2: candidate was already Ready before this run and all gates
          # were freshly re-evaluated above. Rebind head/base immediately before
          # the irreversible repository-history transition.
          current_main="$(gh api "repos/${REPOSITORY}/commits/main" --jq .sha)"
          current_head="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}" --jq .head.sha)"
          current_base="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}" --jq .base.sha)"
          test "$current_main" = "$EXPECTED_BASE"
          test "$current_base" = "$EXPECTED_BASE"
          test "$current_head" = "$EXPECTED_HEAD"

          gh api \
            --method PUT \
            "repos/${REPOSITORY}/pulls/${PR_NUMBER}/merge" \
            -f merge_method=merge \
            -f sha="$EXPECTED_HEAD" \
            > /tmp/qikvrt-promotion-merge.json
          cat /tmp/qikvrt-promotion-merge.json
          test "$(python3 -c 'import json; print(str(json.load(open("/tmp/qikvrt-promotion-merge.json"))["merged"]).lower())')" = "true"

          promoted_main="$(gh api "repos/${REPOSITORY}/commits/main" --jq .sha)"
          test "$promoted_main" != "$EXPECTED_BASE"
          gh pr comment "$PR_NUMBER" --repo "$REPOSITORY" --body "Expected-head promotion completed by repository-native executor. Source head: \`$EXPECTED_HEAD\`; previous main: \`$EXPECTED_BASE\`; promoted main: \`$promoted_main\`. No Zenodo/IETF/release/PASS/FINAL_PASS/EFFECT_ACK_DONE effect is implied."

      - name: Persist bounded decision summary
        if: always()
        shell: bash
        run: |
          {
            echo "## QIK-VRT expected-head promotion"
            echo
            if [ -f /tmp/qikvrt-promotion-decision.json ]; then
              echo '```json'
              cat /tmp/qikvrt-promotion-decision.json
              echo '```'
            elif [ -f /tmp/qikvrt-promotion-candidate.json ]; then
              echo '```json'
              cat /tmp/qikvrt-promotion-candidate.json
              echo '```'
            else
              echo "No promotion snapshot was materialized."
            fi
            echo
            echo "External effects remain fail-closed."
          } >> "$GITHUB_STEP_SUMMARY"
