# QIKVRT V2.13.4AV2A Open Multi-Node Release Freeze HTA Token Fix

Run on Windows:

```cmd
START_HIER_RELEASE_FREEZE_INSTALL.cmd
```

This package supersedes 4AV2 when the masked token HTA dialog reports a syntax error.

Properties: one command, masked local token dialog, no PowerShell execution path, no `.ps1`, no Git command path, no embedded token.
